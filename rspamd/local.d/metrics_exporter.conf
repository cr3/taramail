# Enable Prometheus metrics export
# Metrics are available at http://taramail-rspamd:11334/metrics

# Allow access from Docker networks without authentication
# This is safe because rspamd is not exposed outside Docker network
enabled = true;

# Format: Prometheus (default)
format = "prometheus";
